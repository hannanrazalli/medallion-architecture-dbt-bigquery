name: 'practice1_medallion'
version: '1.0.0'
config-version: 2
profile: 'default'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  practice1_medallion:
    staging:
      +schema: bronze
      +materialized: table  # TUKAR DARI incremental KE table

    intermediate:
      +schema: silver
      +materialized: table  # TUKAR DARI incremental KE table

    marts:
      +schema: gold
      +materialized: table